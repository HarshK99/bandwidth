# Google Calendar integration (optional — see docs/CALENDAR.md).
# Public OAuth client ID from Google Cloud Console → Credentials → OAuth client
# ID (type: Web application). No client secret — the browser uses the GIS token
# flow. Leave unset to disable the calendar feature entirely.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
